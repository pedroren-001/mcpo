version: '3.9'

services:
  mcpo:
    build: .
    container_name: mcpo
    command: [
      "--config", "/app/config/config.json",
      "--hot-reload"
    ]
    volumes:
      - ./config:/app/config
    ports:
      - "19821:8000"
    restart: unless-stopped
